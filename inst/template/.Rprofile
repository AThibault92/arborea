## Permet de charger le pacakge arborea dès que le projet est lancé
if (requireNamespace("arborea", quietly = TRUE)) {
  message("Chargement du .Rprofile pour le projet arborea")
  library("arborea")
} else {
  message("Le package arborea n'est pas installé")
}
